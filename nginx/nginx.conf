events {}

# Define which servers to include in the load balancing scheme.
#https://www.digitalocean.com/community/tutorials/understanding-nginx-server-and-location-block-selection-algorithms-pt

http {

    upstream produtos {
        server atividade_microsservicos_produtos_1:3000;
        server atividade_microsservicos_produtos_2:3000;
     }

    # upstream calc{
    #     server microservicos_calc_1:5000;
    # }

    # upstream soma{
    #     server microservicos_soma_1:5000;
    # }

    # upstream sub{
    #     server microservicos_sub_1:5000;
    # }

# This server accepts all traffic to port 80 and passes it to the upstream.
     server {
         listen 80;
         server_name app.com;

         location /produtos {
              proxy_pass http://produtos;
         }

        #  location /calc {
        #       proxy_pass http://calc;
        #  }

        #  location /soma {
        #       proxy_pass http://soma;
        #  }

        #  location /sub {
        #       proxy_pass http://sub;
        #  }
     }
}